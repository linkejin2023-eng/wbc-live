services:
  - type: web
    name: wbc-live
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: ADMIN_SECRET
        generateValue: true
